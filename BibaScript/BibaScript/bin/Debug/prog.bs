func void Hello(float pivo)
{
	WriteLine("Привет)");
	string i = ReadLine();
	Write(i);
}

func void Pivo(int i, float pivo)
{
	
	bool t = true;
	while (t)
	{
		i = i + 1;
		t = false;
	}
	if (false)
	{
		i = i + 1;
		pivo = pivo + 1;
	}
	:else if (0)
		i = i - 1;
	:else if (Equal(i, 30) || (More(i, 30) && ! false))
	{
		bool g = true;
		string gh = "3" + Hello() + "34" + 34;
		string d = g + gh;
	}
	:else if (0)
		i = 0;
	:else
		pivo = pivo - 1;
	i = 1000;
}
Hello(3);
Pivo(3, 4);